$(document).ready(function(){
	$('.slick-client').slick({
        dots: true,
        dotsClass: 'dots-ecam2',
        //autoplay: true,
        //autoplaySpeed: 5000
    });
    $('.dots-ecam li button,.dots-ecam2 li button').text('');
});